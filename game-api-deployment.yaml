apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-api-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: game-api
  template:
    metadata:
      labels:
        app: game-api
    spec:
      containers:
      - name: game-api-container
        image: isurukavinda99/game_api:latest
        ports:
        - containerPort: 8081
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: DB_HOST
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: DB_PASSWORD
        - name: DB_PORT
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: DB_PORT
        - name: COGNITO_USER_POOL_ID
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: COGNITO_USER_POOL_ID
        - name: COGNITO_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: COGNITO_CLIENT_ID
        - name: AWS_REGION
          valueFrom:
            secretKeyRef:
              name: game-api-secret
              key: AWS_REGION

